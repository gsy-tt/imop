--- a/lib/uloop.c
+++ b/lib/uloop.c
@@ -494,7 +494,7 @@ uc_uloop_timer_remaining(uc_vm_t *vm, siz
 		return NULL;
 	}
 
-	rem = (int64_t)uloop_timeout_remaining(&timer->timeout);
+	rem = (int64_t)uloop_timeout_remaining64(&timer->timeout);
 
 	return ucv_int64_new(rem);
 }
